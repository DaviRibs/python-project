# WhatsApp Bot - Aula de Automação
Este projeto é um bot que responde mensagens automaticamente usando Selenium.

## Como rodar
1. Instale as dependências: `pip install -r requirements.txt`
2. Rode o bot: `python index.py`